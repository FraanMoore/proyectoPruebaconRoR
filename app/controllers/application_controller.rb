class ApplicationController < ActionController::Base #herada la clase del framework rails que contiene la logica para recibir peticiones
                                                    #renderizar HTML, JSON, etc 
end
